{
  "name": "@treemich/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nodemon --config nodemon.json",
    "build": "npm run build -w @treemich/shared && tsc -p tsconfig.json",
    "start": "node dist/src/server.js",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "prisma:generate": "prisma generate --schema prisma/schema.prisma",
    "prisma:migrate:dev": "prisma migrate dev --schema prisma/schema.prisma",
    "phase0:backfill": "tsx scripts/phase0-backfill-legacy-columns.ts",
    "geocode:missing-places": "tsx scripts/geocode-missing-places.ts",
    "phase4:backfill-families": "tsx scripts/phase4-backfill-families.ts"
  },
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "@fastify/rate-limit": "^10.3.0",
    "@prisma/client": "^6.19.3",
    "@treemich/shared": "0.1.0",
    "adm-zip": "^0.5.16",
    "convert-source-map": "^2.0.0",
    "dotenv": "^16.5.0",
    "estree-walker": "^3.0.3",
    "fastify": "^5.2.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.5",
    "@types/node": "^22.14.0",
    "nodemon": "^3.1.10",
    "prisma": "^6.19.3",
    "tsx": "^4.19.3",
    "typescript": "^5.8.3",
    "vite": "8.0.9",
    "vitest": "^4.1.4"
  }
}
