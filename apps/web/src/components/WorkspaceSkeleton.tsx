export const WorkspaceSkeleton = () => (
  <section className="workspace-main-stack">
    <section className="card stack workspace-intro-card" aria-label="Loading workspace">
      <div className="skeleton-card" />
    </section>
  </section>
);
